import { styled } from '..'

export const Container = styled('div', {
  minHeight: '100vh',
  paddingTop: '2rem',
  display: 'flex',
  flexDirection: 'column',
  justifyContent: 'center',
  alignItems: 'flex-start',
})
